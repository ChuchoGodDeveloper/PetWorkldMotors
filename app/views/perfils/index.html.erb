<div class="neo-container">
  
  <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px;">
    <h2 class="neo-title" style="margin: 0;">Gestión de Perfiles</h2>
    
    <% if tiene_permiso?('Perfils', :bitAgregar) %>
      <%= link_to 'Nuevo Perfil', new_perfil_path, class: "neo-button", style: "width: auto; padding: 10px 20px; text-decoration: none;" %>
    <% end %>
  </div>

  <div style="margin-bottom: 30px; text-align: left;">
    <label class="neo-subtitle" style="display: block; margin-bottom: 10px; font-weight: bold;">BUSCAR POR NOMBRE</label>
    <input type="text" id="buscador-input" placeholder="🔍 Buscar perfil..." class="neo-input" style="margin: 0; width: 100%; max-width: 400px;" autocomplete="off">
  </div>

  <table style="width: 100%; border-collapse: collapse; text-align: left;">
    <thead>
      <tr style="border-bottom: 1px solid var(--bento-border);">
        <th style="padding: 15px 10px; color: var(--text-secondary);">#</th>
        <th style="padding: 15px 10px; color: var(--text-secondary);">NOMBRE DEL PERFIL</th>
        <th style="padding: 15px 10px; text-align: center; color: var(--text-secondary);">ADMINISTRADOR</th>
        <th style="padding: 15px 10px; text-align: center; color: var(--text-secondary);">ACCIONES</th>
      </tr>
    </thead>
    <tbody>
      <% @perfils.each do |perfil| %>
        <tr class="fila-busqueda" data-search="<%= perfil.strNombre_Perfil.downcase %>" style="border-bottom: 1px solid var(--bento-border);">
          <td style="padding: 15px 10px; color: var(--text-secondary);"><%= perfil.id %></td>
          <td style="padding: 15px 10px; font-weight: bold; color: var(--text-primary);"><%= perfil.strNombre_Perfil %></td>
          <td style="padding: 15px 10px; text-align: center; color: var(--text-secondary);">
            <%= perfil.bitAdministrador ? 'Sí' : 'No' %>
          </td>
          <td style="padding: 15px 10px; text-align: center;">
            <div style="display: flex; gap: 10px; justify-content: center;">
              
              <% if tiene_permiso?('Perfils', :bitEditar) %>
                <%= link_to 'Editar', edit_perfil_path(perfil), class: "neo-button", style: "width: auto; padding: 8px 15px; text-decoration: none;" %>
              <% end %>

              <% if tiene_permiso?('Perfils', :bitEliminar) %>
                <%= button_to 'Eliminar', perfil_path(perfil), method: :delete, form: { data: { turbo_confirm: '¿Estás seguro?' } }, class: "neo-button", style: "width: auto; padding: 8px 15px; color: var(--danger); border-color: rgba(239, 68, 68, 0.2); background: rgba(239, 68, 68, 0.1);" %>
              <% end %>

            </div>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <div style="margin-top: 20px;">
    <%= paginate @perfils if defined?(paginate) %>
  </div>
</div>

<script>
  document.getElementById('buscador-input').addEventListener('input', function (e) {
    const term = e.target.value.toLowerCase();
    const rows = document.querySelectorAll('.fila-busqueda');

    rows.forEach(row => {
      const searchData = row.getAttribute('data-search');
      if (searchData.includes(term)) {
        row.style.display = ''; 
      } else {
        row.style.display = 'none'; 
      }
    });
  });
</script>